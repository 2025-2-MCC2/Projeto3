// Copie este arquivo para env.js e ajuste a URL da API conforme o ambiente
// Ex.: em produção (Netlify/Vercel), aponte para o backend publicado (Render/Railway)
window.__ENV = {
  API_URL: "http://localhost:3001"
};
